第二章：类型、运算符与表达式

变量和常量是程序处理的两种基本对象。声明语句说明变量的名字及类型，也可以指定变量的初值。
运算符指定将要进行的操作。表达式则把变量与常量组合起来生成新的值。对象的类型决定该对象
可取值的集合以及可以对该对象执行的操作。

所有整型都包括signed（带符号）和unsigned（无符号）两种形式，且可以表示无符号常量与十六进制字符常量。
浮点数运算可以以单精度进行，还可以使用更改精度的long double类型。
字符串常量可以在编译时连接。
对象可以声明为const（常量）类型，表明其值不能修改。